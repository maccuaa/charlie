name: Deploy
on:
  schedule:
    - cron: 0 */3 * * *
jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: curl -X POST -d {} ${{ secrets.NETLIFY_BUILD_HOOK }}
